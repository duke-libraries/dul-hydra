<% if object_children.response.total_pages > 1 %>
 <div class="row record-padding">
  <div class="span9"> 
    <div class="pagination">
      <%= paginate object_children.response, :params => pagination_params, :outer_window => 2, :theme => 'blacklight' %>
    </div>
  </div>
 </div>
<% end %>
