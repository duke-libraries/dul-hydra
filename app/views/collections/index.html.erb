<h1>Collections</h1>

<ul>
  <% @collections.each do |collection| %>
    <li><%= link_to collection.title.first.length > 0 ? collection.title.first : collection.pid , collection_path(collection) %></li>
  <% end %>
</ul>