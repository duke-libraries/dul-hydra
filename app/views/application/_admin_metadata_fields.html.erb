<table class="table">
  <% admin_metadata_fields.each do |field| %>
  <tr>
    <th scope="row"><%= t("dul_hydra.admin_metadata.#{field}", default: field.to_s.titleize) %></th>    
    <td><%= render_admin_metadata_field field %></td>
  <tr>
  <% end %>
</table>
