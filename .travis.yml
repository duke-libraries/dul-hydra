language: ruby
rvm:
  - 1.9.3
before_script:
  - "cp config/log4r.yml.sample config/log4r.yml"
  - "rake jetty:clean"
  - "rake jetty:config"
script: "rake dul_hydra:ci"
branches:
  only:
    - travis-ci
